---
title: "JavaScript Fundamentals"
description: "JavaScript powers interactivity on the web. Understanding its core concepts is essential before moving to frameworks like React."
tags: ["javascript", "basics", "fundamentals"]
estimatedTime: "90 minutes"
youtubeId: "hdI2bqOjy3c"
fullCode:
  title: "JavaScript Basics Example"
  description: "A script illustrating variables, functional array transformations, closures, and async/await API fetching."
  language: "javascript"
  code: |
    // 1. Data Structures & Functional Transformations
    const users = [
      { id: 1, name: "Alice", role: "admin", active: true },
      { id: 2, name: "Bob", role: "student", active: false },
      { id: 3, name: "Charlie", role: "student", active: true }
    ];

    // Filter active students and extract their names
    const getActiveStudentNames = (userList) => {
      return userList
        .filter(user => user.active && user.role === "student")
        .map(({ name }) => name);
    };

    console.log("Active Students:", getActiveStudentNames(users));

    // 2. Variable Scope & Closures
    const createCounter = () => {
      let count = 0; // encapsulated state variable
      return {
        increment: () => ++count,
        decrement: () => --count,
        getCount: () => count
      };
    };

    const counter = createCounter();
    counter.increment();
    console.log("Count value:", counter.getCount()); // output: 1

    // 3. Asynchronous Operations using async/await
    const fetchUserData = async (userId) => {
      try {
        const response = await fetch(`https://jsonplaceholder.typicode.com/users/${userId}`);
        if (!response.ok) {
          throw new Error(`HTTP error! status: ${response.status}`);
        }
        const data = await response.json();
        const { name, email, company: { name: companyName } } = data;
        return { name, email, companyName };
      } catch (error) {
        console.error("Error fetching user data:", error.message);
        return null;
      }
    };
---

## Variables and Types

```javascript
// Modern variable declaration
const name = "Alice";           // Use const by default
let age = 25;                   // Use let if reassignment needed
var old = true;                 // Avoid var (function-scoped, confusing)

// Types
const number = 42;
const string = "hello";
const boolean = true;
const array = [1, 2, 3];
const object = { name: "Alice", age: 25 };
const nothing = null;
const undefined_value = undefined;
```

<SeniorNote>
In production JavaScript, always use `const` by default. Use `let` only when you need reassignment. `var` is legacy and causes scope confusion.
</SeniorNote>

## Functions

```javascript
// Arrow functions (modern)
const add = (a, b) => a + b;

// Traditional function
function multiply(a, b) {
  return a * b;
}

// Functions with default parameters
const greet = (name = "Guest") => `Hello, ${name}`;
```

## Arrays and Objects

```javascript
// Array methods
const numbers = [1, 2, 3, 4, 5];
numbers.map(n => n * 2);        // Transform
numbers.filter(n => n > 2);     // Filter
numbers.find(n => n === 3);     // Find first

// Object destructuring
const person = { name: "Alice", age: 25 };
const { name, age } = person;
```

<Pitfall>
Mutating shared objects in place causes bugs that are hard to trace. Prefer immutable updates with object spread or array methods.
</Pitfall>

## Async Programming

```javascript
// Promises
const fetchData = () => {
  return fetch("/api/data")
    .then(response => response.json())
    .then(data => console.log(data))
    .catch(error => console.error(error));
};

// Async/await (modern, cleaner)
const getData = async () => {
  try {
    const response = await fetch("/api/data");
    const data = await response.json();
    console.log(data);
  } catch (error) {
    console.error(error);
  }
};
```

## Key Takeaways

- Use `const` and `let`, never `var`
- Arrow functions are standard in modern code
- Master async/await for handling asynchronous operations
- Learn array methods (map, filter, reduce)

<Checklist
  title="JavaScript Fundamentals Checklist"
  items={[
    "Use const and let correctly",
    "Understand functions and scope",
    "Know arrays and objects",
    "Avoid mutating shared state",
    "Handle errors explicitly"
  ]}
/>

This foundation is critical before learning React!
