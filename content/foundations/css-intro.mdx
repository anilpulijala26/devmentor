---
title: "CSS Fundamentals"
description: "CSS (Cascading Style Sheets) transforms plain HTML into beautiful, responsive designs. In 2026, modern CSS has evolved significantly—gone are the days of browser hacks and complex workarounds."
tags: ["css", "styling", "layout"]
estimatedTime: "60 minutes"
youtubeId: "OXGznpKZ_sA"
fullCode:
  title: "CSS Box Model & Selectors Example"
  description: "A style sheet demonstrating low-specificity BEM styling, responsive breakpoints, and custom CSS properties."
  language: "css"
  code: |
    /* Custom Design Tokens */
    :root {
      --primary-color: #4f46e5;
      --primary-hover: #4338ca;
      --text-color: #1e293b;
      --background-color: #ffffff;
      --border-radius-lg: 12px;
      --spacing-md: 16px;
      --font-family: system-ui, -apple-system, sans-serif;
    }

    /* Base Elements */
    body {
      font-family: var(--font-family);
      color: var(--text-color);
      background-color: #f8fafc;
      margin: 0;
      padding: var(--spacing-md);
      box-sizing: border-box;
    }

    *, *::before, *::after {
      box-sizing: inherit;
    }

    /* BEM Card Component showing the Box Model */
    .card {
      background: var(--background-color);
      border: 1px solid #e2e8f0;
      border-radius: var(--border-radius-lg);
      padding: 24px;
      margin: 20px auto;
      max-width: 480px;
      box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    .card:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
    }

    .card__title {
      font-size: 1.25rem;
      font-weight: 700;
      margin: 0 0 12px 0;
    }

    .card__description {
      font-size: 0.875rem;
      color: #64748b;
      line-height: 1.5;
      margin-bottom: 20px;
    }

    .card__action {
      display: inline-flex;
      align-items: center;
      background-color: var(--primary-color);
      color: #fff;
      text-decoration: none;
      font-weight: 600;
      font-size: 0.875rem;
      padding: 10px 20px;
      border-radius: 8px;
      transition: background-color 0.2s ease;
    }

    .card__action:hover {
      background-color: var(--primary-hover);
    }

    /* Responsive adjustments */
    @media (max-width: 640px) {
      .card {
        padding: 16px;
        margin: 10px;
      }
    }
---

## Selectors and Specificity

```css
/* Element selector */
p { color: blue; }

/* Class selector */
.button { padding: 10px; }

/* ID selector (avoid in production) */
#header { background: white; }

/* Attribute selector */
input[type="email"] { border: 1px solid gray; }

/* Combinators */
nav > ul { list-style: none; }
article p { margin: 1rem 0; }
```

<SeniorNote>
Specificity wars are common in production codebases. Use BEM (Block, Element, Modifier) or utility-first frameworks like Tailwind to avoid CSS conflicts. Don't rely on high specificity to fix problems.
</SeniorNote>

## The Box Model

```css
div {
  width: 200px;
  padding: 20px;      /* Inside the box */
  border: 1px solid;
  margin: 10px;       /* Outside the box */
}
```

Remember: **margin** is outside, **padding** is inside.

## Display and Layout

```css
/* Block, inline, inline-block */
div { display: block; }
span { display: inline; }
button { display: inline-block; }

/* Flexbox */
.container { 
  display: flex;
  gap: 1rem;
  align-items: center;
}

/* Grid */
.grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 2rem;
}
```

<ProTip>
Flexbox and Grid are now standard. Don't use floats for layout—that's 2010s thinking. Modern CSS is declarative and powerful.
</ProTip>

## Responsive Design

```css
/* Mobile first */
body { font-size: 14px; }

/* Tablet and up */
@media (min-width: 768px) {
  body { font-size: 16px; }
}

/* Desktop and up */
@media (min-width: 1024px) {
  body { font-size: 18px; }
}
```

<Pitfall>
Overusing complex selectors makes CSS brittle. Prefer class-based selectors and keep specificity low.
</Pitfall>

<Checklist
  title="CSS Fundamentals Checklist"
  items={[
    "Understand the box model",
    "Keep specificity low",
    "Use mobile-first breakpoints",
    "Prefer Flexbox and Grid for layout",
    "Avoid !important"
  ]}
/>

## Key Takeaways

- Learn Flexbox and Grid, not floats
- Use a CSS framework or methodology (Tailwind, BEM)
- Keep specificity low
- Mobile-first approach
- Use CSS custom properties (variables)

This is a foundational lesson. Dive deeper by building projects!
